# === 深度学习框架（API 变化较大，需迁移代码）===
#tensorflow==2.20.0
#torch==2.7.0        # pip install torch --index-url https://download.pytorch.org/whl/cpu

# === 原废弃包的替代品 ===
scikit-learn==1.6.1     # 替代 sklearn==0.0（同样 import sklearn）
cvxpy==1.6.0            # 替代 cvxpy==0.4.11（API 有变化）
openpyxl==3.1.5         # 替代 xlrd + xlwt
jupyterlab==4.3.4       # 替代 notebook==5.7.8（或用 notebook==7.x）
# simplegeneric → 已内置 functools.singledispatch，无需安装
# webencodings → 作为 html5lib 的间接依赖，无需直接列出

# === 需升级版本 ===
matplotlib==3.10.3
statsmodels==0.14.4
Pillow==11.2.1
tables==3.10.2
TA-Lib==0.6.8
pymssql==2.3.2
pymongo==4.10.1
requests==2.32.3
cvxopt==1.3.2
chardet==5.2.0
redis==5.2.1
XlsxWriter==3.2.3
simplejson==3.19.3
tqdm==4.67.1

# === 直接兼容 ===
appdirs==1.4.4
dbf==0.99.0
multitasking==0.0.12
lxml==5.4.0
backtrader==1.9.78.123  # ⚠ 停止维护，有风险，见下方说明

# === 数据库 ===
pymongo==4.10.1
#pymssql==2.3.2
redis==5.2.1